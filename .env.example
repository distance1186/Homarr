# Homarr Environment Variables
# Copy this file to .env and update with your values
# The .env file is gitignored for security

# Timezone (change to your local timezone)
TZ=America/New_York

# Optional: Database configuration (if using external database)
# DATABASE_URL=postgresql://user:password@host:5432/homarr

# Optional: Authentication settings (if configuring auth)
# AUTH_SECRET=your-secret-key-here

# Optional: Custom port (if you want to change from default 7575)
# PORT=7575

# Optional: Base URL (if behind reverse proxy)
# BASE_URL=https://homarr.yourdomain.com

# Add any service-specific API keys or credentials here
# PLEX_TOKEN=your-plex-token
# UNIFI_USERNAME=your-username
# UNIFI_PASSWORD=your-password
